# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
out/
.next/
.turbo/
target/
**/test_snapshots/
*.wasm
*.tsbuildinfo
next-env.d.ts

# Internal planning docs. These hold business strategy (pricing, GTM, moat).
# The repo is public, so they are kept local by default. Remove these lines if
# the team decides to publish them.
Solva-PRD-1.md
Solva-PRD-2.md

VERCEL.md

# Noir / Barretenberg artifacts
**/target/*.json
**/proofs/
**/*.gz
# Auto-generated witness input template. Real inputs come from the prover.
circuits/**/Prover.toml

# Go
bin/
*.test
*.out
vendor/

# Environment
.env
.env.local
.env.*.local
*.key
*.pem

# Editor
.vscode/
.idea/
*.swp
.DS_Store

# Logs
*.log
npm-debug.log*

# The proof-registry bindings are committed (src + built dist) so the wrapper
# builds without the Stellar CLI or network. Regenerate with `just gen-bindings`,
# then rebuild dist. Only the sub-package's installed deps are ignored.
packages/contract-bindings/src/generated/node_modules/

# Stellar
.stellar/

#Cargo
Cargo.lock
