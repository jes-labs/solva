# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
out/
.next/
.turbo/
target/
**/test_snapshots/
*.wasm
*.tsbuildinfo
next-env.d.ts

# Internal planning docs. These hold business strategy (pricing, GTM, moat).
# The repo is public, so they are kept local by default. Remove these lines if
# the team decides to publish them.
Solva-PRD-1.md
Solva-PRD-2.md

VERCEL.md

# Noir / Barretenberg artifacts
**/target/*.json
**/proofs/
**/*.gz
# Auto-generated witness input template. Real inputs come from the prover.
circuits/**/Prover.toml

# Go
bin/
*.test
*.out
vendor/

# Environment
.env
.env.local
.env.*.local
*.key
*.pem

# Editor
.vscode/
.idea/
*.swp
.DS_Store

# Logs
*.log
npm-debug.log*

# The generated proof-registry bindings: the source is committed so the wrapper
# builds without the Stellar CLI or network; its dist is a build artifact that
# the contract-bindings build/typecheck regenerates (and so stays ignored).
# Regenerate the source with `just gen-bindings`.
packages/contract-bindings/src/generated/node_modules/

# Prover runtime artifacts: the compiled circuit and the BN254 CRS, assembled
# by `just build-artifacts`. Generated and large (CRS), never committed.
/artifacts/

# Stellar
.stellar/

#Cargo
Cargo.lock
