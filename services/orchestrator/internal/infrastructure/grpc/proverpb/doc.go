// Package proverpb holds the generated gRPC stubs for the prover service.
//
// The files in this package are generated from /proto/prover.proto by
// `just proto` (buf), which runs protoc-gen-go and protoc-gen-go-grpc. Do not
// hand-write the .pb.go files; regenerate them from the proto after a change.
// They are committed so builds and CI need no codegen step. The proto's
// go_package option targets this exact import path:
//
//	github.com/jes-labs/solva/services/orchestrator/internal/infrastructure/grpc/proverpb
//
// The prover client in the parent package programs against the generated
// ProverClient interface and the ProveRequest/ProveResponse messages.
package proverpb
