# The sandbox playground (/sandbox) drives a running Solva backend server-side.
# Point these at a local stack during a demo, or a hosted one later.
ORCHESTRATOR_URL=http://localhost:8080
SANDBOX_URL=http://localhost:8090

# Stellar explorer base for the on-chain proof links.
NEXT_PUBLIC_STELLAR_EXPLORER=https://stellar.expert/explorer/testnet
